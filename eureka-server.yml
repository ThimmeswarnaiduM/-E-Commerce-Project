server:
  port: 8761
  
eureka:
  instance:
    hostname: eurekaServer  # Matches your Docker container name

  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://eurekaServer:8761/eureka
