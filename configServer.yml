server:
  port: 8888

spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ThimmeswarnaiduM/your-repo-name
          label: main
          clone-on-start: true
          force-pull: true
          timeout: 5000
         
