spring:
  data:
    mongodb:
      username: admin
      password: admin
      host: localhost
      port: 27018
      database: customer
      authentication-database-name: admin
      uri: mongodb://admin:admin@localhost:27018/customer?authSource=admin&authMechanism=SCRAM-SHA-1
 logging:
  level:
    org.springdoc: DEBUG
    org.springframework.web: DEBUG

# Correct springdoc properties (note the top-level placement)
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

